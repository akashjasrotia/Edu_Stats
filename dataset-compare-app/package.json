{
  "name": "dataset-compare-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "cd client && npm install",
    "server": "cd server && npm install",
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "dev": "vite --host"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "devDependencies": {}
}